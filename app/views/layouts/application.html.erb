<!DOCTYPE html>
<html>
<head>
	<title>Todolys - <%= yield :title %></title>
	<%= stylesheet_link_tag "reset" %>
	<%= stylesheet_link_tag "text" %>
	<%= stylesheet_link_tag "960" %>
	<%= stylesheet_link_tag "application" %>

	<%= javascript_include_tag :defaults %>
	<%= csrf_meta_tag %>
</head>
<body>
	<div class="container_12">
		<div class="grid_3">
			<%= link_to "Logo", root_path %>
		</div>
		<div class="grid_9">  
			<% if current_user %>  
			<%= link_to "Edit @" + current_user.username , edit_user_path(:current) %>  
			<%= link_to "Logout", logout_path %>  
			<% else %>  
			<%= link_to "log into you @account", login_path %>  
			<% end %> 
		</div>
	</div>
	<div class="container_12">
		<p id="notice" class="grid_12"><%= notice %></p>	
		<%= yield %>
	</div>
	<div class="container_12">
		<div class="grid_2 prefix_10 footer">
			&copy; Todolys 2010
		</div>
	</div>
</div>
</body>
</html>
